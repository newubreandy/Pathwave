# l10n — ARB fallback 파일 디렉터리

Phase 1 에서 지원하는 10개 언어의 ARB 파일이 여기에 들어옵니다.

- ko, en, zh-CN, ja, zh-TW, vi, th, tl, id, ms

I18nService 는 네트워크/캐시가 모두 실패할 때 이 ARB 를 fallback 으로 사용합니다.
실제 ARB 파일 추가는 Phase 1 다국어 작업 시 진행합니다.
